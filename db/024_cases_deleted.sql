-- Удалённый проект должен пропадать из выбора (ГП, списки), а не висеть
-- в журнале как живой. Помечаем запись, а не удаляем: история стадий и
-- строка журнала остаются, но проект больше нигде не предлагается.
ALTER TABLE cases ADD COLUMN IF NOT EXISTS deleted_at TIMESTAMPTZ;

CREATE INDEX IF NOT EXISTS cases_deleted_idx ON cases (deleted_at);

-- Разовая чистка: проекты, папки которых уже нет на диске, пометить
-- удалёнными нельзя — файловой системы отсюда не видно. Это делает
-- сервер при первом запуске (см. src/server.js, markLostCases).
