-- ============================================================
--  Добавляет поле "место бронирования" — чтобы при подтверждении
--  бронирования оно автоматически переносилось в "место использования",
--  без повторного ввода.
--
--  Запуск:
--    cat db/005_booked_where.sql | docker compose exec -T db psql -U pribory -d pribory
--
--  (Если у вас уже есть файл с номером 005 — переименуйте этот в следующий
--  свободный номер; сам SQL внутри от этого не меняется.)
-- ============================================================

ALTER TABLE instruments ADD COLUMN IF NOT EXISTS booked_where text;
