# Настройки с паролями — НИКОГДА не должны попасть в репозиторий
.env
.env.local
*.env

# Реальные файлы в файловом менеджере и его служебная база данных —
# это данные, а не код, версионировать их в git не нужно
filestore/

# Зависимости Node (их ставит Docker при сборке)
node_modules/

# Резервные копии базы
*.sql.gz
backup*/

# Служебное
.DS_Store
Thumbs.db
*.log
